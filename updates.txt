I added a duplicate detector to add prompts for dupe UPC's
Start with a node file (not cmd or powershell) and type node server.js to start the server

I updated Worthington Styles
Changed product order
I replaced FS with USDA

Please:
Add a "View Full Nutrition" link at the bottom of the Nutrition Facts popup after clicking on nutrition facts,
then link that to the full nutrition page (preferably usda)

Develop a retail scraper (Amazon) for images
If that fails, find a different approach
If you cannot, we will consult Cory at a separate time (make this our last option)

Fix all the remaining little things or leave them to me
Find something creative
If you cannot do this on your own, feel free to use Copilot or Codex

Commit and push

LOG YOUR WORK HERE:

2026-06-07: Implemented local server USDA integration and image scraper.
- Added USDA API key support in `server.js` (dotenv), normalized `/nutrition` responses.
- Added lightweight Amazon image scraper `scraper/amazon.js` (uses node-fetch + cheerio). CLI: `node scraper/amazon.js --asin <ASIN>` or `--query "name"`.
- Added `cheerio` dependency and `wn-scrape-amazon` bin entry in `package.json`.
- Installed deps and started server for local testing.

Notes:
- Scraper first tries `og:image` and `data-a-dynamic-image`; may fail for heavily-protected pages. If Amazon scraping fails, use product images from OpenFoodFacts or placeholder images in `images/`.
- DO NOT COMMIT your USDA API key. Rotate the key since it was pasted in chat.